package Thread;

// every sleep method throws interruptedexception which is checked exception hence whenever we are using sleep method compulsery we should handel interrupted exception either by try catch or throws keyword otherwise we will get compile time error
// if a thread don't want to perform any operation for a particular amount of time then we should go for sleep method
public class SleepMethod {

}
